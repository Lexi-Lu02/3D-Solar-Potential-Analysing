# Production — frontend and API are served from the same nginx on EC2,
# so a relative path is correct. The browser resolves it against the current origin.

VITE_API_BASE_URL=http://3.26.146.10/api/v1
VITE_GEOJSON_URL=http://3.26.146.10/combined-buildings.geojson
VITE_PRECINCTS_URL=http://3.26.146.10/melbourne_cbd_precincts.geojson
